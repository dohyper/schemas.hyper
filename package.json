{
  "name": "@dohyper/schemas",
  "version": "0.0.7",
  "description": "",
  "main": "source/index.js",
  "scripts": {
    "development": "nodemon --watch './*.js' --exec npm run build",
    "build": "npm run build:bundle && npm run build:lockin && npm run build:types",
    "build:bundle": "node bundle.js",
    "build:lockin": "node lockin.js",
    "build:types": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "M'bark Erras",
  "license": "ISC",
  "dependencies": {
    "@apidevtools/json-schema-ref-parser": "^11.5.4",
    "json-schema-to-ts": "^3.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "typescript": "^5.4.5"
  },
  "types": "./build/index.d.ts"
}
